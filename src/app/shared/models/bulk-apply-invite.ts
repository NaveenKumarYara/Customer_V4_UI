import { CandidateInformation } from ".";

export class BulkApplyInvite {
    JobId: number;
    SelectedCandidates: CandidateInformation[];

    // constructor(jobId: any, selectedCandidates: any)
    // {
    //     this.JobId = jobId;
    //     this.SelectedCandidates = selectedCandidates;
    // }
}